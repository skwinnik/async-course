{
  "appConfiguration": {
    "RabbitConnectionString": "host=localhost:5672;virtualHost=/;username=user;password=qwerty",
    "KafkaBootstrapServers": "localhost:9092",
    "SqlConnectionString": "Host=localhost;Port=5432;Database=AuthService;Username=postgres;Password=qwerty"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://*:5000"
      }
    }
  }
}
